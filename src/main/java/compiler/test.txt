//Good luck
int a;
bool g;
if (a == 1 && false == g) {
    //
}
int i;
i = (i+2)*-2%3;
int[] c = int[5];  // new array of length 5

final string message = "Hello";
final bool run = true;

struct Point {
    int x;
    int y;
}

int[] x;
a = -3;
int b = x[readInt() * 2 + 2];

def int square(int v) {
    return v*v;
}

for (i = 0, 10 > i *2 +1 , i = 2) {
    int z = 69;
}

struct Person {
    string name;
    Point location;
    int[] history;
}

final float j = 3.2*5.0;
//final k int = i*3;
//final string[] message = "Hello";
final bool isEmpty = true;

def Point copyPoints(Point[] p) {
    return Point(p[0].x+p[1].x, p[0].y+p[1].y);
}

def void main() {
    int value = readInt();
    writeInt(square(value));
    int i;
    for (i=1, i*2 <100+4*readInt(), i = i+1*4+readInt()) {
        while (value!=3*2+1) {
            if (i*4+2 > 10){
                // ....
            } else {
                // ....
            }
        }
    }
    i = (i+2)*2 + 1+2*2;
}